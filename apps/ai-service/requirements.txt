# geolocation/projection.py and validation/ have zero external deps beyond
# the stdlib (math, dataclasses) -- kept minimal on purpose.

pytest>=8.0.0

# HTTP surface for POST /ai/geolocate (role doc Section 7 "APIs exposed").
# Not required for the CLI demo (geolocate.py) or the test suite.
fastapi>=0.110.0
uvicorn>=0.27.0

# tracking/kalman_tracker.py: constant-velocity Kalman filter + Hungarian
# (linear_sum_assignment) data association. tracking/tracker.py (greedy
# centroid matcher) has no dependency beyond stdlib and remains available.
numpy>=1.26.0
scipy>=1.11.0
