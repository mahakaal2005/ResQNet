# detection (owner: Faiqua)
POST /ai/detect + CLI batch mode (python detect.py --image sample.jpg)
Output contract: Detection object (Section 10.2), co-signed with Rudra.
Mock fixture to publish Week 1: apps/ai-service/mocks/mock_detections.json
Independent demo: no backend, no simulator required.
