# missions (owner: Charan)
POST /missions, GET /missions/:id, PATCH /missions/:id/status
Table: missions. Publishes: mission.started, mission.paused, mission.completed.
Mock fixture to publish Week 1: packages/contracts/mocks/mock_missions.json
