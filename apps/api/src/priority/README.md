# priority (owner: Rudra)
GET /incidents/:id/priority-breakdown
Table: priority_scores. Weights documented in docs/contracts/priority-weights.md.
Ayush renders this breakdown verbatim — never recompute client-side.
